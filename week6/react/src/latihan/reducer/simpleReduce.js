//this one is reducer
const adder = (total, number) => {
  return total + number
};

let numbers = [1,2,3];
let some = numbers.reduce(adder, 0);

//step 1
const adder = (1, 0)
adder = (total = initialState, number)
return 0 + 1 // hasi total = 1

adder = (total = 1, number = 2)
return 1 + 2 //hasil total  = 3

adder = (total = 3, number = 3)
return 3 + 3 //hasil total  = 6

//state total  = 6

console.log(some)